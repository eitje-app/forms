export const labels = {
  remote_avatar: 'Choose remote avatar',
}

export const general = {
  required: 'required',
  invalid: 'invalid'
}

const form = {...general, label: labels}

export default form