export const fields = {
  default_avatar: 'Choose remote avatar',
}

export const names = {
  team: 'Team'
}

export const general = {
  required: 'required',
  invalid: 'invalid'
}

const form = {...general, field: fields, name: names}

export default form